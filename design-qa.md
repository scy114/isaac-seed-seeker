# Design QA

## 对照目标与证据

- 视觉真值：`docs/design/visual-target.png`
- 最终实现截图：`output/design-audit/09-post-fix-desktop.png`
- 窄屏截图：`output/design-audit/10-post-fix-mobile.png`
- 最终全屏并排对照：`output/design-audit/11-post-fix-comparison-full.png`
- 最终标题与表单局部对照：`output/design-audit/12-post-fix-comparison-header-form.png`
- 状态：默认“目标 169”条件，名称目录及本地游戏图标已载入，页面位于顶部。

## 视口与归一化

- 桌面 CSS 视口：`1440 × 1024`，`devicePixelRatio ≈ 1`。
- 原始视觉真值：`1487 × 1058 px`。
- 浏览器实现截图：`1425 × 1013 px`；浏览器截图接口排除了滚动条轨道，可用内容宽度为 `1425 px`。
- 对照时将视觉真值以 Lanczos 等比例缩放到 `1425 × 1013 px`，再与实现逐边并排；没有额外裁切。
- 窄屏 CSS 视口：`390 × 844`；页面内容宽 `375 px`，无横向溢出。

## 最终对照结论

全屏与局部对照都显示，页面已经保留目标图的核心视觉层级：居中的红色刀刃标题、右上纸片存档状态、地下室背景、厚边框黑色告示板、WANTED 与 A/B/C 纸片标签、暗红筛选条目和紧凑的三栏/两栏布局。实现中的条件文字和道具条目使用真实应用数据，因此没有复制概念图里虚构或错误的道具内容。

### 必查表面

- 字体与排版：艺术标题作为透明位图完整保留；IsaacSans 用于纸片标签，中文使用系统可读字体。标题、区块标题和辅助文字的层级明确，移动端没有截断。
- 间距与布局：桌面标题、存档纸条与主面板的比例已接近视觉真值；筛选区更紧凑是为了容纳真实输入。`390 px` 窄屏按单列重排，未发现横向溢出或控件遮挡。
- 色彩与视觉令牌：暗褐地下室、近黑面板、干血红、脏纸粉/米色与目标一致；绿色仅用于“目录已就绪”状态。
- 图像质量与素材：标题透明边缘无绿幕光晕；纸片、地下室和道具图标读取自本机游戏资源，保持像素渲染。没有用 SVG、emoji、CSS 图形或占位符替代可见资产。
- 文案与内容：保留真实版本、真实筛选规则和真实道具名称；没有把概念图的幻觉内容带入产品。

## 交互与运行检查

- “清空条件”将筛选 token 从 8 个降为 0；“恢复目标 169”恢复为 8 个。
- 本地目录、游戏图标、地下室纹理、纸片和标题接口均通过冒烟检查。
- 核心预测测试与 2000 万种子搜索冒烟测试通过；命中数仍为 3，首个种子仍为 `B74H HQPR`。
- 浏览器控制台日志为空。

## 对照迭代历史

1. 初次对照发现 `[P2]` 标题图虽然有 `790 px` 容器，但 `object-fit: contain` 因刀身总高度把可见文字压缩到约一半宽度；同时右侧存档卡参与 flex 排版，使标题向左偏。
2. 修正：把存档卡改为右上绝对定位纸条；标题以整页为中心，按 `700 × 148 px` 目标槽位展开；中窄屏与移动端分别降级为安全尺寸和单列布局。
3. 修后证据：`11-post-fix-comparison-full.png` 和 `12-post-fix-comparison-header-form.png` 显示标题重新成为首屏视觉中心，存档卡与标题不重叠，主面板起始位置保持稳定。此前的 P2 已关闭。

## 剩余 P3 润色

- 概念图背景含额外墙面涂鸦与一个红色小角色；实现选择了玩家本机的真实地下室地砖，没有继续生成第二批装饰素材。这是有意的素材来源取舍，不影响层级或使用。
- 实现纸片偏粉，概念图偏旧米色；前者来自游戏原始 `seed paper.png`，保留更直接的游戏辨识度。

## 最终结果

final result: passed
